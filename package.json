{
  "name": "bedrocktools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.mjs && bun scripts/extract-data.mjs && bun scripts/translate-items.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "dev": "bun run build && bun scripts/serve.mjs",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {
    "fflate": "^0.8.1",
    "minecraft-data": "^3.112.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.35.0"
  }
}
